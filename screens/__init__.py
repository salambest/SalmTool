"""
SalmTool Ultimate - screens package.

Each file here is one KivyMD MDScreen: a thin UI layer (KV markup +
callbacks) that calls into the corresponding module under `modules/` for
actual analysis logic. Keeping UI and logic separate is what makes it easy
to add a 13th, 14th, ... feature screen later without touching anything
else.
"""
